(defun square (x)  ;; equivalent to x^2
  (* x x))
(defun add (x)    ;; equivalent to x+x
  (+ x x))
(defun subtract (x) ;;equivalent to 10-x
  (- 10 x))
(defun cube (x)   ;;equivalent to x^3
  (* x (* x x)))
